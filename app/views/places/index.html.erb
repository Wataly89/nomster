
<%= will_paginate @places, previous_label: h("<"), next_label: h(">") %>

<% @places.each do |place| %>
	<div class="booyeah-box col-10 offser-1">
		<h1><%= place.name %></h1>
		<i><%= place.address %></i><br /><br />
		<p><<%= place.description %></p><br />
		</div>

	<hr />

<% end %>

